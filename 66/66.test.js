const function66=require('../66/66')

test('should return percentage of students who scored below the class average marks in at least one subject.', () => {
    expect(function66()).toStrictEqual("75%")
})