const function88=require('../88/88')

test('should return  the percentage of students who scored below the average marks of the class in at least one subject.', () => {
    expect(function88()).toBe("75%")
})