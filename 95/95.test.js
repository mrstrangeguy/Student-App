const function95=require('../95/95')

test('should return  calculate and print the percentage of students who scored above the average marks of a specific student in the majority of subjects.', () => {
    expect(function95("Aju")).toBe("25%")
})