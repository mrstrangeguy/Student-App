const function91=require('../91/91')

test('should return  the student(s) who scored above the average marks of the class in the majority of subjects.', () => {
    expect(function91()).toStrictEqual([ 'Binu' ])
})