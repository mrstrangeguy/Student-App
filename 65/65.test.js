const function65=require('../65/65')

test('should return the the percentage of students who scored above the class average marks in at least one subject.', () => {
    expect(function65()).toStrictEqual("100%")
})