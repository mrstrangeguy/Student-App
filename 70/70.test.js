const function70=require('../70/70')

test('should return student(s) who scored below the class average marks in the majority of subjects', () => {
    expect(function70()).toStrictEqual([ 'Ravi', 'Aju', 'Mini SS' ])
})